import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ComandaScreen = () => {
	return (
		<View>
			<Text>This is the comanda screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default ComandaScreen;
